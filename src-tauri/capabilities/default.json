{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default app capability for local Birds Eye windows.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
